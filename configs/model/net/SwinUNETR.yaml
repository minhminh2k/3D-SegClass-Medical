_target_: src.models.swin-unetr.components.SwinUNETR

img_size: [128, 128, 128]
in_channels: 4
out_channels: 3
feature_size: 48
spatial_dims: 3
drop_rate: 0.0,
attn_drop_rate: 0.0,
dropout_path_rate: 0.0,
use_checkpoint: False # gradient checkpointing for reduced memory usage
