_target_: monai.losses.DeepSupervisionLoss

loss:
  # _target_: src.models.losses.monai_losses.DiceCELoss
  # include_background: True
  # sigmoid: False
  # to_onehot_y: True
  # softmax: True
  # squared_pred: False
  # jaccard: False
  # smooth_nr: 0.00001
  # smooth_dr: 0.00001
  # lambda_dice: 1.0
  # lambda_ce: 1.0

  _target_: src.models.losses.focal_tversky_3d.AsymmetricUnifiedFocalLoss

  to_onehot_y: True
  num_classes: ${model.num_classes}
  weight: 0.3 # focal / 1 - weight * tver
  gamma: 1.0
  delta: 0.8 # fore ce / fn
  softmax: True
  

weight_mode: "exp"
# weights: [1, 0.5, 0.25, 0.125, 0.1]