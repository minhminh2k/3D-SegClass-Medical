_target_: monai.losses.DeepSupervisionLoss

loss:
  _target_: src.models.losses.monai_losses.DiceCELoss
  include_background: True
  sigmoid: True
  softmax: False
  squared_pred: False
  jaccard: false
  smooth_nr: 0.00001
  smooth_dr: 0.00001
  lambda_dice: 1.0
  lambda_ce: 1.0

  # _target_: src.models.losses.focal_tversky_3d.AsymmetricUnifiedFocalLoss
  # to_onehot_y: False
  # num_classes: 2
  # weight: 0.5
  # gamma: 0.5
  # delta: 0.7
  
  # _target_: src.models.losses.focal_tversky_3d.FocalTverskyLoss
  # alpha: 0.7
  # beta: 0.3
  # gamma: 0.75

weight_mode: "exp"
# weights: [1, 0.5, 0.25, 0.125, 0.1]